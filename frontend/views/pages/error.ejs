<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>No se encontraron los datos</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #f4f4f4;
      color: #333;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
      text-align: center;
    }

    .container {
      max-width: 500px;
      padding: 30px;
      background-color: #fff;
      border-radius: 10px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
    }

    .emoji {
      font-size: 80px;
    }

    h1 {
      font-size: 26px;
      margin: 20px 0 10px;
    }

    p {
      font-size: 16px;
      color: #666;
    }

    .btn {
      margin-top: 20px;
      padding: 10px 20px;
      background-color: #ff4d4f;
      color: white;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      text-decoration: none;
      font-size: 14px;
      transition: background-color 0.3s ease;
    }

    .btn:hover {
      background-color: #d9363e;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="emoji">🚧😒</div>
    <h1>No se encontraron los datos</h1>
    <p>Algo salió mal... puede que estés intentando acceder a información que no existe o hubo un problema interno. ERR 500</p>
  </div>
</body>
</html>
